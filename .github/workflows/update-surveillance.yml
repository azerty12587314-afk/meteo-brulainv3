name: Update surveillance
on:
  workflow_dispatch:
  schedule:
    - cron: "5,20,35,50 * * * *"
permissions:
  contents: write
concurrency:
  group: surveillance-update
  cancel-in-progress: true
jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install requests
      - run: python scripts/update_surveillance.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add surveillance/data.json
          if git diff --cached --quiet; then echo "No changes"; else git commit -m "chore: update surveillance" && git push; fi
